<%= link_to "Back to Products Index", products_url %> | <%= link_to "Edit", edit_product_path(@product) %> | <%= link_to "Delete", delete_product_path(@product), method: :delete, data: {confirm: "Are you sure you want to delete this product?"} %>

<p><b><%= product.name.capitalize %></b> - <%= product.description.capitalize %></p>
  <p><%= product.price_in_cents %> Cents</p>